pipeline {
    agent any
    /* {
        docker {
            images { 'node:lts-buster-slim' 

                        
            args '-p 3000:3000' 
        }
    }*/
     environment {
        CI = 'true'
    }
    stages {
        stage('Build') { 
            agent{
                docker {
                    image 'node:lts-buster-slim'
                }
            }
            steps {
                sh 'npm config set registry https://registry.npmjs.org/'
                sh 'npm  cache clear --force'
                
            }
        }
        stage('Test') {
            agent{
                docker {
                    image 'python'
                }
            }
            steps {
               // sh 'pip -h install virtualenv --user'
                sh 'pip -H install selenium --user'
                sh 'python reacttest.py'
            }
        }  
        
        
        
    }       
}