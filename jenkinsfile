pipeline {
    agent any
    /* {
        docker {
            images { 'node:lts-buster-slim' 

                        
            args '-p 3000:3000' 
        }
    }*/
     environment {
        CI = 'true'
    }
    stages {
        stage('Build') { 
            agent{
                docker {
                    image 'node:lts-buster-slim'
                }
            }
            steps {
                sh 'npm config get proxy'
                sh 'npm config rm proxy'
                sh 'npm config rm https-proxy'
                sh 'npm install'
                sh 'npm start'
                
            }
        }
        stage('Test') {
            agent{
                docker {
                    image 'python'
                }
            }
            steps {
                sh 'python3 -m venv env'
                sh 'python3 -m pip install selenium --user'
                sh 'python reacttest.py'
            }
        }  
        
        
        
    }       
}
